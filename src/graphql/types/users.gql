input LoginInput {
  email: String!
  password: String!
}

type LoginPayload {
  id: Int!
  email: String!
  accessToken: String!
}

type User {
  id: Int!
  name: String!
}

type Mutation {
  login(input: LoginInput!): LoginPayload!
}
